# Section Instructions: Review Methodology

## Agent Rules
- Do NOT finalize this section until `scope.md` is confirmed by human.
- Must align with `resources/search_strategy/search_strategy.md`.
- Must include a PRISMA-style flow description (refer to `resources/prisma_diagram/template.md`).

## Methodology Approach (Updated — NO PRISMA CLAIM)
This review uses a structured, transparent methodology inspired by systematic review practices (search strategy, inclusion/exclusion criteria, screening, data extraction) but does NOT claim formal PRISMA protocol registration or full PRISMA compliance. The PRISMA-style flow diagram (see `resources/prisma_diagram/template.md`) is included as an organizational and visual aid only — not as proof of registered protocol.

Agents must state this clearly: "This review follows a structured methodology with transparent reporting of search, screening, and selection. It does not claim formal PRISMA protocol registration."
2. **Databases Searched** (List from `search_strategy.md`; add final search dates).
3. **Search Strategy** (Copy/refine from resources; include syntax per database).
4. **Inclusion / Exclusion Criteria** (Reference `resources/inclusion_criteria/criteria.md`; state final criteria).
5. **Screening Process** (Who screened? Single/double? How were disagreements resolved?)
6. **Data Extraction** (What variables were extracted? — use a table or list.)
7. **Quality Assessment / Bias** (How was study quality assessed?)

## PRISMA Diagram
- Once screening numbers are finalized, create `resources/prisma_diagram/diagram.tex` or `.png`.
- Reference the figure in this section.

## Pushback Points
- If no protocol was registered, state this clearly — don't claim PRISMA compliance if it isn't fully met.
- If screening was not double-blind, note this as a limitation in `challenges_future`.
