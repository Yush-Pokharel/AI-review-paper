# Section Instructions: Waste Detection Approaches

## Agent Rules
- Structure progressively: Classification -> Object Detection -> Semantic Segmentation -> Instance Segmentation.
- For each: Definition, typical architectures, advantages, limitations, when to use for waste detection.

## Pushback Points
- Do not conflate object detection (bounding box) with instance segmentation (pixel mask + instance ID). Keep definitions strict.
- If literature mostly uses object detection but you find emerging instance segmentation work, highlight this gap.
- Ensure citations reference actual studies, not just architecture papers.
