# Conclusion — Notes (no new studies; restate + review limitations + contribution)

- Purpose restated: generalization of DL waste detection across riverine environments (RQ-R).
- Findings by section: platforms (§4), approaches (§5), architectures (§6: YOLO workhorse, transformer gap), datasets (§7: open-resource turn), training (§8: task-dependent optima + transfer + SSL/synthetic/UDA), evaluation (§9: counting gap), gaps (§10), directions (§11).
- Review limitations: seed-anchored (not exhaustive DB); single-agent screening; English-only; provisional rows; 2019–2025 window; underwater absent by literature, not by choice.
- Base-paper relation: supports + extends P001 (cross-river finding replicated across 19 A-grades; training-strategy finding qualified by architecture/task).
- Contribution: first generalization-centered synthesis with A/B/C/D evidence grading across 50 studies.
