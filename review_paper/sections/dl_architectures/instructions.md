# Section Instructions: Deep Learning Architectures

## Agent Rules
- Analyze each family: YOLO (v3-v8+, RT-DETR), Faster R-CNN, SSD, U-Net / U-Net++, DeepLab (v1-v3+), Mask R-CNN, Vision Transformers (ViT, Swin, DeiT), newer architectures (SAM, ConvNeXt, etc.).
- For each: Key innovation, typical waste-detection performance (if reported), strengths, weaknesses, speed vs. accuracy trade-off.
- Create comparison table or figure if helpful.

## Required References
- Cross-check with `base_published_paper/references.bib` for any architecture citations that should be reused or updated.
- If the base paper focuses on one architecture (e.g., YOLO), emphasize how other architectures compare in this review.

## Pushback Points
- Don't just list architectures — compare them specifically for waste detection tasks (small objects, occlusion, underwater visibility, etc.).
- If newer architectures (transformers, SAM) have very few waste-detection studies, flag this as a gap.
